# Ignore node_modules (where Tailwind and its dependencies are installed)
node_modules/

# Ignore the default build output directories (e.g., dist, build)
dist/
build/

# Ignore the default Tailwind config files (if generated by the CLI)
tailwind.config.js

# Ignore package-lock.json or yarn.lock files (optional, only if you prefer to ignore them)
package-lock.json
yarn.lock

# Ignore the default PostCSS config file (if used in the project)
postcss.config.js

# Ignore any other autogenerated files (optional)
*.log
*.temp
package.json
